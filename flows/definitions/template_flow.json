{
    "Comment": "Template flow definition — replace with your actual flow logic",
    "StartAt": "TransferStep",
    "States": {
        "TransferStep": {
            "Type": "Action",
            "ActionUrl": "https://actions.automate.globus.org/transfer/transfer",
            "Parameters": {
                "source_endpoint_id.$": "$.input.source_endpoint_id",
                "destination_endpoint_id.$": "$.input.destination_endpoint_id",
                "transfer_items": [
                    {
                        "source_path.$": "$.input.source_path",
                        "destination_path.$": "$.input.destination_path",
                        "recursive": true
                    }
                ]
            },
            "ResultPath": "$.TransferResult",
            "End": true
        }
    }
}
