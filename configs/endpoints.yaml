# Globus endpoint and collection mappings
# Fill in UUIDs for your environment

endpoints:
  source:
    display_name: "Source Endpoint"
    uuid: ""
    base_path: "/data/"

  destination:
    display_name: "Destination Endpoint"
    uuid: ""
    base_path: "/output/"

  # Add more endpoints as needed
  # facility_endpoint:
  #   display_name: "Facility Storage"
  #   uuid: ""
  #   base_path: "/"
